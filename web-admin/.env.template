# Copy to .env (untracked) and adjust per environment.

# Backend base URL for the B2B API.
VITE_API_BASE_URL=http://localhost:8000

# WebSocket base URL, used for realtime pricing updates.
VITE_WS_BASE_URL=ws://localhost:8000/pricing/ws
